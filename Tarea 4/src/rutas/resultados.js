// Requerir express
const express = require('express');
const router = express.Router();

// Requerir controlador
//const controlador = require('./../controladores/resultados')

// Listado específico
//router.get('/', controlador.verNoticia);

module.exports = router; // Es un middleware